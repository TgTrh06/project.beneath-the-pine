# Profile

Owns optional display name and validated IANA timezone. Profile identifiers come from the authenticated principal. Device timezone may be proposed by a client but is only stored after an explicit update.
