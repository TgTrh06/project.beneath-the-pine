# Migration output reserved

Chưa có migration SQL hoặc journal. Drizzle Kit output được cấu hình tới thư mục này. Cần review SQL baseline hiện hữu, ownership, schema và môi trường thử nghiệm trước khi generate/apply. Không dùng migration lịch sử như journal mới tự động.
