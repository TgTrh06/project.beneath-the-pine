# Public web configuration. Leave VITE_API_URL unset to use the local demo.
# Enable it after the corresponding API routes exist.
# VITE_API_URL=http://localhost:8081/api/v1

# Server-only API configuration lives in apps/api/.env.example.
