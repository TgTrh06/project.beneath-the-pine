# Backup and Recovery

Before persisting pilot data, choose provider backup retention, restore test cadence and account deletion/backups policy. A restore must preserve ownership and lifecycle consistency; rollback must not drop user data solely to undo a release.
