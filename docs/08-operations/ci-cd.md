# CI/CD

Run typecheck/lint/test/build appropriate to changed client/API code. Realtime/database integration tests must not silently skip in CI. Documentation-only changes require Markdown-link and stale-term checks. Deployment remains a separately authorized action.
