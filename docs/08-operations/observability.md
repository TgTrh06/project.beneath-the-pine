# Observability

Observe API latency/errors, auth denial, database connectivity, websocket connection/reconnect, active-session state transition failure and export/delete completion. Product events cover seed resume, pact attendance and repeat pact without raw text. Configure alerts only for capabilities actually deployed.
